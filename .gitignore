var/
__pycache__/
*.pyc
engine/tools/sp_daemon/target*/
engine/tools/sp_swarm/target/
# G-CLEAN-BUILD outputs + the lib junction (build-cuda-backend.bat recreates it)
engine/build-cpu/
engine/build-host-cuda-backend/
engine/lib/
# B4-NIGHTSHIFT live episodes: the kairos binary bakes engine_root at compile
# time (routes.rs env!(CARGO_MANIFEST_DIR)/../..) — production data, never repo
# content. Unify under var/ when P1b makes the episode dir config.
engine/_nightshift_live/
var/voice/wav/
var/voice/*.npz
var/voice/*.log

# session scratch (G-VERBATIM cleanup 2026-07-12)
_*.py
_*.log
_*.err
_*.out
_*.ps1
core)
var/commitmsg.txt
var/hidden/

# Runtime telemetry written by every gate run — records, not source.
# It dirtied git status after each run, which trains you to ignore git status,
# and a status you ignore is a status that hides a real change.
memory-okf-telemetry/

# STORED MEMORY SURFACES (operator, 2026-07-15): her stores are hers, not repo
# content. The synthetic fixtures under harness_tests/fixtures/ remain tracked.
memory-okf/
memory-okf-self/
memory-okf-personality/
persona.md
